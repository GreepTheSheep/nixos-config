**The "Jax-server-VM" host is a server test VM that runs on the main computer**

Configuration:
- AMD Ryzen 7 7700X (8 cores enabled)
- 8GB RAM enabled
- 1 SCSI disk enabled at 64GB:
  - ext4 filesystem mounted at "/"