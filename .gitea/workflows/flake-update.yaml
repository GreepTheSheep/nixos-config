name: Flake Update

on:
  schedule:
    # Every Friday at 19:12 UTC
    - cron: "12 19 * * 5"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: unstable
          fetch-depth: 0
          persist-credentials: false

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Add Github Token
        run: |
          mkdir -p ~/.config/nix
          echo "access-tokens = github.com=${{ secrets.GH_TOKEN }}" > ~/.config/nix/nix.conf

      - name: Update flake.lock
        run: nix flake update

      - name: Commit and push
        env:
          BOT_USERNAME: ${{ vars.ACTIONS_BOT }}
          BOT_TOKEN: ${{ secrets.ACTIONS_BOT_TOKEN }}
        run: |
          if git diff --quiet flake.lock; then
            echo "flake.lock is already up to date"
            exit 0
          fi

          git config user.name "$BOT_USERNAME"
          git config user.email "$BOT_USERNAME@users.noreply.gitea.com"
          git add flake.lock
          git commit -m "Flake update ($(date +%F))"

          git remote set-url origin "https://${BOT_USERNAME}:${BOT_TOKEN}@${GITHUB_SERVER_URL#https://}/${GITHUB_REPOSITORY}.git"
          git push origin HEAD:unstable